name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    # ArxDb.Tests не має Windows-специфічних залежностей і не посилається
    # на жоден сестринський репозиторій (на відміну від ArxLang) — єдина
    # ОС достатня. ArxLang підключає ArxDb через ProjectReference, тож
    # регресія тут інакше виявилась би лише непрямо, під час збірки ArxLang.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Build
        run: dotnet build src/ArxDb --configuration Release

      - name: "Run test suite (24 перевірки: WAL, crash-recovery, компакція, конкурентність, fuzz)"
        run: dotnet run --project tests/ArxDb.Tests --configuration Release
